import Vue from "vue";
import Vuex from "vuex";

import $_common from "./common";

Vue.use(Vuex);

export default new Vuex.Store({
  state: {},
  getters: {},
  mutations: {},
  actions: {},
  modules: {
    $_common,
  },
});
